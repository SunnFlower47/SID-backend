# Development Memory - 26 May 2026

## Konteks & Diskusi
- Warga meminta **Export Excel Kepala Keluarga** diurutkan secara hierarki berdasarkan wilayah (RT, RW, Dusun) agar rapi secara geografis saat dibaca/dicetak.
- Variabel Cetak Surat khusus Penduduk Domisili disederhanakan (`domisili_` menjadi `dm_`) agar lebih mudah diingat.
- Diskusi terkait tombol **Perpanjang** & **Cabut** pada status domisili:
  - Tombol Perpanjang selalu aktif kapan saja (sebelum atau sesudah expired). 
  - Aksi "Perpanjang" yang tadinya *redirect* ke halaman form surat diubah menjadi murni eksekusi API *1-click*, kemudian UI *refresh*.
  - Aksi "Cabut" menonaktifkan status domisili selamanya (tidak bisa diperpanjang lagi).
- Diskusi terkait layout: Tampilan *Modal* dirasa kurang memadai untuk membaca detail data domisili, sehingga direfaktor menjadi sebuah *Page* mandiri (`Show.jsx`) bergaya "Card Summary" (serupa dengan `Penduduk/Show.jsx`) untuk konsistensi UI/UX.

## Solusi & Implementasi Teknis
1. Update `app/Exports/KartuKeluargaExport.php` untuk memodifikasi query Eloquent menggunakan `orderByRaw("CAST(rts.kode AS UNSIGNED)")` untuk pengurutan numerik yang akurat.
2. Refactor komponen UI Domisili:
  - Ekstraksi *Modal* menjadi `Tenant/Domisili/Show.jsx`.
  - Mengimplementasikan layout 1/3 (kiri profil) dan 2/3 (kanan info form).
  - Melakukan *parsing date* untuk UI (menggunakan `date-fns`) agar raw datetime API ditampilkan rapi.
3. Update routing (`tenant/kependudukan.php`) dengan `Route::get('/{domisili}', 'show')` dan *method* di Controller.
4. Perubahan perilaku `handlePerpanjang` dari `router.get` (buka halaman) menjadi `router.post(route('domisili.perpanjang', id))` (eksekusi latar belakang langsung).
5. Update file *package.json* dari `1.6.1-beta` menjadi `1.6.2`.
